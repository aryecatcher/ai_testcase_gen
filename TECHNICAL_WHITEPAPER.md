# AI 测试用例生成系统：技术白皮书 (V2.0 生产级演进)

## 1. 系统架构总览

本系统已从早期的线性脚本演进为**高并发、有状态、具备自我进化能力**的准生产级 Agent 架构。系统采用前后端分离设计，核心逻辑通过多步工作流驱动。

### 核心技术栈
- **前端展示**: Streamlit (提供实时交互与指标监控)
- **后端服务**: FastAPI (高性能异步 REST 接口，支持 SSE 流式传输)
- **状态管理**: LangGraph (驱动有状态的 AI 思考过程)
- **持久化层**: SQLModel + SQLite (结构化存储需求与用例)
- **知识引擎**: NetworkX + Neo4j (混合图谱驱动的业务约束感知)

---

## 2. 核心 Agent 工作流 (LangGraph)

系统不再依赖单次 LLM 调用，而是通过一个**闭环有向图**来实现高质量生成：

1.  **Context Retrieval (上下文检索)**: 从知识图谱中根据模块名提取业务规则、字段约束和典型测试场景。
2.  **Initial Draft (初稿生成)**: 结合需求文本与图谱知识，利用 LLM 生成第一版测试用例。
3.  **Self-Correction & Validation (自我修正与校验)**: 
    - 运行正则表达式校验数据格式（手机号、邮箱等）。
    - 对照知识图谱中的“禁止/必须”规则进行硬性检查。
    - 自动识别步骤与预期结果之间的逻辑矛盾。
4.  **Refinement Loop (优化循环)**: 如果校验发现缺陷（如缺失边界值或逻辑错误），自动带上反馈意见触发二次生成。

---

## 3. 生产级特性

### 3.1 高性能并发与流式传输
- **异步 I/O**: 基于 `AsyncIO` 的非阻塞架构，支持自定义并发信号量（Semaphore）。
- **多模型协同 (Multi-Model Orchestration)**: 系统支持针对不同任务分配不同量级的模型。例如：使用轻量级模型进行初稿生成，而强制使用高参数量的模型（如 DeepSeek-R1-14B/32B）担任“AI 判官”节点，以确保审计质量。
- **实时追踪 (SSE)**: 通过 Server-Sent Events 将 Agent 的每一步思考路径（Trace）实时推送到 UI，消除“黑盒”疑虑。

### 3.2 知识自学习 (Self-Learning)
- **结构化提取**: 用户对用例的修正意见会触发 LLM 自动精炼，提取出原子化的业务规则。
- **历史反馈沉淀**: 系统支持从某个需求下所有用例的历史修正记录中，批量提取并精炼通用规则，极大地提高了知识库的填充速度。
- **持久化沉淀**: 新规则会自动存入知识图谱，实现“一次纠错，全系统永久受益”。

### 3.3 系统仪表盘与审计溯源 (Observability & Audit)
- **实时仪表盘**: 实时统计全量 Token 消耗、平均生成耗时及知识图谱命中率。
- **批量执行监控**: UI 提供精细化的执行视图，实时展示每个需求的执行节点（Retrieve/Generate/Judge/Optimize）与状态。
- **需求级审计 (Per-Requirement Audit)**: 每个需求的生成过程（Thinking Trace）与消耗的 Token 均被持久化至数据库。

### 3.4 知识图谱浏览器 (KG Browser)
- **可视化管理**: 提供专门的“知识图谱”面板，区分展示“全局业务基线”与“模块化规则”。
- **全局基线 (Global Baseline)**: 展示跨模块的通用规则（如安全红线、全局性能要求），这些规则会被所有生成任务自动继承。

### 3.5 多样化导出与自动化集成
- **Excel/Feishu/Postman**: 支持多种传统办公与协作工具的导出。
- **Pytest 脚本导出**: 一键将生成的自然语言用例转化为可执行的 Python (Pytest) 脚本，包含自动化生成的断言占位符、Pytest Markers 及日志追踪逻辑，大幅缩短从设计到自动化的距离。

---

## 4. 性能指标参考 (Beta 测试)

| 指标 | 提升/数据 | 说明 |
| :--- | :--- | :--- |
| **生成效率** | > 40% | 相比纯人工编写，单条需求平均生成耗时显著降低 |
| **知识命中率** | 75% - 85% | 随着用户自学习的积累，图谱对业务的覆盖度持续提升 |
| **幻觉率** | < 5% | 经过正则拦截与工作流自我修正后的逻辑错误率 |
| **高并发支持** | 50+ 需求/批次 | 异步架构支持大规模需求的一键处理 |

---

## 5. 未来展望
- **模型微调 (Fine-tuning)**: 针对特定行业术语进行 RAG 或微调优化。
- **多智能体协作**: 引入专门的“安全 Agent”、“性能 Agent”进行更深度的专项测试设计。
- **自动化执行集成**: 与 Pytest/Selenium 等框架打通，实现从生成到执行的全自动化。

---
*文档版本: 2.0.0*
*更新日期: 2026-04-03*
